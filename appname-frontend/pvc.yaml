# AppName Frontend 用共有 PVC（Tekton の source/cache workspace で共有）
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: appname-frontend-pipeline-pvc
  labels:
    app.kubernetes.io/part-of: appname-cicd
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 5Gi
